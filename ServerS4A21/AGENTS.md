# ServerS4A21 统一工作流

本文件是 `ServerS4A21` 项目的统一协作入口，适用于人工开发、AI 修改、代码审查、测试、文档、数据库和发布操作。

## 1. 规则优先级与适用范围

进入任何项目后，按以下顺序确定规则：

1. 系统、开发者和用户在当前任务中的明确要求。
3. 项目内的 `CONTRIBUTING.md`、README、专项规范、设计文档和测试说明。
3. 本统一工作流中的通用规则。

## 2. 开始任务前的强制检查

### 2.1 确认真实工作区

- 确认当前目录、项目根目录、目标文件和仓库边界。
- 查找并阅读适用的`CONTRIBUTING.md`、README 和目标业务专项文档。
- Git 项目首先执行 `git status --short --branch`，识别已有修改、未跟踪文件和当前分支。
- 不得回滚、覆盖、暂存或提交用户及其他协作者的无关改动。
- 参考目录、旧服务端、旧客户端和历史代码默认只读；除非用户明确要求，不得写入或同步覆盖。

### 2.2 定位业务真源

开始编辑前必须确认：

- 真实文件、命名空间、注册入口和调用链。
- 业务数据的唯一 owner、运行时真源和持久化边界。
- 请求解析、应用编排、领域规则、Repository、事务提交和协议通知各自的职责。
- 现有同类 Service、Repository、Coordinator、缓存、随机源、计时器和测试能否复用。
- 数据库、协议、PVF/资源、客户端行为和生成文件的影响范围。

禁止仅凭文件名、类名、方法名或相邻代码猜测业务语义。

### 2.3 建立最小任务边界

- 一次任务只解决一个明确问题或一个可独立验收的批次。
- 先写清预期行为、失败行为、验证方法和不在本次修改范围内的内容。
- 证据不足时标记“待确认”，先做只读调查，不以猜测扩大实现。
- 不得顺手混入无关重构、全目录格式化、批量改名或生成文件更新。

## 3. 修改过程统一规则

### 3.1 编辑纪律

- 手工代码和文档修改使用 `apply_patch` 或等价的可审阅小补丁。
- 禁止未经审阅的全局替换、整目录格式化和大范围机械改写。
- 保持补丁小而集中；修改共享代码时同时检查原有调用方和回归范围。
- 不得复制一套相似 Service、Repository、缓存、随机源、状态机或生命周期管理器来绕开现有设计。
- 不得为了通过编译删除校验、吞异常、放宽长度限制、伪造默认数据或绕过事务边界。
- 不得截断必要代码、测试结果或修改说明；交付内容必须完整。

### 3.2 数据和持久化

- 先确认当前数据库基线和初始化策略，再修改 schema 或迁移。
- 不得恢复项目明确淘汰的种子库、历史迁移、默认账号或运行时隐式建表。
- 新数据库应由当前基线 schema 或项目规定的初始化代码创建。
- 只有新功能确实需要结构变化时才新增连续迁移；已发布迁移不得改写、重排或复用版本号。
- 多表组成的一次业务写入必须使用明确的事务边界。
- 不得在协议 handler 中直接写 SQL，或绕过项目现有 Repository/CommitService/Coordinator。
- 运行数据库、玩家存档、数据库 WAL/SHM、测试数据和备份默认不得提交。

### 3.3 协议、客户端和资源证据

- 协议字段、顺序、长度、flag 和消费条件必须有抓包、客户端逆向、当前资源或既有验证实现等证据。
- A21 适配使用的 CMD/NOTI 包名、opcode 和枚举归类必须从 `Server/DfoServer/Network/Core/PacketTypesA21.cs` 的 `CmdPacketTypeA21`、`NotiPacketTypeA21` 获取；禁止在业务代码或文档中重新维护一套 A21 数字常量/名称表。
- `PacketTypesA21.cs` 的枚举值不是运行时包序。包的实际发生顺序必须以当前 A21 抓包、客户端逆向或已验证 SelfTest 为证据，并在时序文档中记录。
- 旧服务端和其他版本只作行为线索，不能覆盖当前客户端或当前项目真源。
- PVF、客户端资源和冻结二进制默认只读；未经用户明确要求不得修改、替换或重新打包。
- 资源哈希、地址和历史分析结论只能证明对应批次；使用前必须确认它们与当前工作区一致。
- 不能自动化覆盖的客户端 UI、断线、重连和时序场景，必须明确写为待实机验证，不能宣称已经完成。

### 3.4 异步、计时器和运行时状态

- 异步回调必须绑定当前 session、角色、房间、run、generation、version 或 ticket，并在执行时重新校验。
- 旧 generation、旧房间或旧副本回调不得修改当前状态。
- 计时器只负责进程内提醒和短阶段推进；不能凭空创建奖励、货币、次数或持久进度。
- 需要跨重启正确的状态必须持久化绝对时间或 durable state，并在启动、登录或恢复时重新计算。
- 不得另起无限循环、后台线程或第二套周期任务系统绕开项目统一调度器。

## 4. 文件和产物边界

以下内容默认不得进入 Git：

- `.ssh/`、私钥、访问令牌、密码和本机凭据。
- `bin/`、`obj/`、`dist/`、`publish/`、`out/`、IDE 缓存和临时目录。
- `*.db`、`*.db-wal`、`*.db-shm`、运行日志、抓包、dump 和玩家存档。
- 项目规定由运行环境提供的 PVF、客户端二进制和大型资源文件。
- 本机绝对路径配置、用户级工程文件和未审核的生成结果。

提交前必须同时检查 `.gitignore` 和实际暂存区；“文件被忽略”不能替代对 `git diff --cached --name-only` 的审查。

清理生成目录前必须确认解析后的绝对路径位于目标工作区内。禁止对不明来源路径执行递归删除、移动或强制覆盖。

## 5. 测试与验收工作流

修改后按风险由小到大验证：

1. 静态检查、格式检查或编译检查。
2. 与本次修改直接相关的聚焦测试。
3. 共享组件、数据库、协议或公共业务路径的回归测试。
4. 项目规定的全量 SelfTest 或完整构建。
5. 需要时由用户执行客户端实机验证。

验收要求：

- 测试必须使用项目规定的解释器、SDK、架构和环境变量。
- 记录实际执行的命令、结果、警告和未覆盖范围。
- 构建或测试失败时先定位真实原因，不得通过删除业务校验伪造通过。
- 清理测试生成的数据库、日志、Debug/Release 输出和发布目录，但不得删除被其他进程占用的未知产物。
- 最终执行 `git diff --check`、`git status --short --branch`，并复查数据库、协议、资源和生成文件是否有非预期变化。

## 6. AI 提交与远程操作规则

- 未经用户明确要求，不主动 commit、merge、rebase、push、force push 或修改远端。
- 用户要求提交时，只暂存本任务文件，并先审查 `git diff --cached --stat` 和文件清单。
- 禁止使用 `git reset --hard`、强制 checkout、强制 clean 或其他破坏性操作处理不明工作区变化。
- 不得修改已经发布的提交历史，除非用户明确要求且影响已经说明。
- 远端存在开仓模板或说明提交时，应优先保留其历史，并让当前项目工作树成为后续提交；不得无说明覆盖远端历史。
- 推送后核对远端引用、提交图和工作区状态，确认本地分支与目标远端一致。

提交或 MR 描述至少包含：

- 修改目标和业务 owner。
- 主要文件和事务边界。
- 数据库、协议、PVF/资源和兼容性影响。
- 实际运行的测试及结果。
- 已知问题、失败尝试、未完成项和实机验证边界。

## 7. 文档

- 修改公共业务规则、数据库基线、协议边界或团队流程时，同步改写对应开发规范正文。
- 开发规范只记录当前生效规范，不得写入更新记录。修改说明写入提交或 MR 描述；未要求独立草案或纠错日志时，不新建冗余文件。
- 文档必须与当前实现、测试和实际目录一致；删除文档时同时清理失效链接。
- 不得把未经验证的推断写成已完成事实。

## 8. 完成条件

只有同时满足以下条件才可宣称任务完成：

- 业务所有权、调用链和事务边界清楚。
- 成功、失败、重复、回滚、旧 generation 和重连场景已按风险验证。
- 聚焦测试和必要回归测试通过，构建达到项目要求。
- 数据库、协议、PVF/资源和生成文件没有非预期变化。
- 暂存区不包含密钥、运行数据、Debug/Release 输出或无关文件。
- 规则变化时规范正文已改写；修改说明写在提交或 MR 中。
- 明确列出仍需用户实机确认或因外部占用无法完成的事项。

## 9. `ServerS4A21` 项目专项入口

在 `ServerS4A21` 工作时，必须先阅读并遵守项目内 `AGENTS.md`。最低阅读清单：

1. `Docs\服务端业务开发规范.md`
2. `CONTRIBUTING.md`
3. 修改 Dungeon、Quest-Dungeon、Settlement、Tower、Tournament 或 Blood Altar 时，阅读 `Docs\副本架构业务接入规范.md`
4. 修改 Inventory、GM、ItemCore、装备、宠物或仓库时，阅读 `Docs\新版背包架构业务接入规范.md` 和 `Docs\GM工具_新背包表结构与ItemCore语义.md`
5. 修改 ExpertJob、分解、附魔、炼金或控偶时，阅读 `Docs\副职业架构业务接入规范.md`
6. 新增或修改 timer、在线恢复或周期任务时，阅读 `Docs\ClockService.md`
7. 阅读目标业务域对应的 SelfTest

项目硬规则：

- 当前 SQLite 基线是 `86jp-database-v1 / schema v1`；不得运行时隐式建表。
- 数据库不存在时，由当前代码和 `Server/DfoServer/Sqlite/item_schema.sql` 创建完整新库；后续结构变化从新体系 schema v2 起追加迁移。
- 在线 Inventory 必须使用当前 owned `InventoryLease`，写入必须经过现有 Repository、CommitService 或 Coordinator 的事务边界。
- 协议修改必须有当前客户端抓包、逆向、PVF 或既有验证实现证据。
- 修改 Dungeon、Quest、Settlement 或共享 Inventory 路径时，显式设置 `PVF_ARCHIVE_PATH`，串行运行聚焦 SelfTest 和 `--selftest-all`。
- `ClockService` 只负责进程内提醒和短阶段推进，不是跨重启持久化真源。
- SelfTest 必须串行执行，因为共享 `Server/DfoServer/bin/Debug/server.log`。
- 最终 Rebuild 要求 `0 warning / 0 error`。
- 开发规范只记录规范，不得写入更新记录。
- `Script.pvf`、SQLite、`bin/obj/dist/publish`、服务端草案和独立纠错日志不得提交。

## 10. 统一执行摘要

```text
确认工作区
  -> 检查 git status 和现有改动
  -> 阅读业务规范与对应 SelfTest
  -> 定位真源、owner、调用链和事务边界
  -> 建立最小可验证修改范围
  -> 使用小补丁实现
  -> 串行运行聚焦测试和必要回归
  -> 构建、diff check、产物与敏感文件检查
  -> 规则变化时改写规范正文
  -> 仅在用户明确要求时提交或推送
  -> 核对最终状态并报告未完成边界
```
